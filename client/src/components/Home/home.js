import React from 'react';
import HomeContainer from '../../containers/home';

const Home = () => {
    return (
        <div className="container">
            <HomeContainer />
        </div>
    );
};

export default Home;